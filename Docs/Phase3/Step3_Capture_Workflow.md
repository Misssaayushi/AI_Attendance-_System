# Step 3: Capture Workflow & Success States

## Goal
Link the camera capture to the form status and display success indicators.

## Tasks
- [x] Implement the `captureFace` function (drawing to canvas).
- [x] On success:
  - Update `isFaceCaptured` state.
  - Show a "Face Captured Successfully" badge.
  - Display a small thumbnail preview of the captured image.
- [x] Add a "Retake" button to reset the capture.
- [x] Trigger a subtle "Flash" animation on the camera feed during capture.

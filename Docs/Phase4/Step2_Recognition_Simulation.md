# Step 2: Recognition Simulation & HUD Overlay

## Goal
Implement the "Scanning" visual effects and mock recognition triggers.

## Tasks
- [x] Add a CSS "Scanline" animation that moves vertically across the camera feed.
- [x] Create a `MatchFound` popup that appears briefly when a face is "recognized."
- [x] Implement a `simulateRecognition` function to test the UI flow.
- [x] Ensure the overlay is responsive and resizes with the camera feed.

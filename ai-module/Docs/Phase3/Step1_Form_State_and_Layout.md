# Step 1: Form Structure & State Management

## Goal
Set up the base `Register.jsx` to handle the multi-step state and initialize the form data object.

## Tasks
- [x] Initialize `formData` state with all 10 fields.
- [x] Implement a `currentStep` state (1, 2, or 3).
- [x] Create a responsive 2-column grid layout for the form.
- [x] Add "Next" and "Previous" navigation logic.
- [x] Use `<Container>` for consistent page width.

## Files Affected
- `src/pages/Register.jsx`

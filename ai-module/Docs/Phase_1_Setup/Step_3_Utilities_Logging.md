# Step 3: Logging & Utility Architecture

## Objective
Implement reusable helper functions and a structured logging system to monitor the AI module's health and capture errors.

## Tasks
- Initialize `ai_module/utils.py`.
- Implement a `Logger` class that writes both to the console and `logs/ai_system.log`.
- Create frame manipulation helpers (resizing, adding text overlays).

## Expected Outcome
A modular `utils.py` that provides common services to all current and future AI scripts.

# Step 5: Testing & Validation

## Objective
Verify that the environment setup is correct and the webcam hardware is communicating properly with the software.

## Tasks
- Create `ai_module/tests/test_system.py`.
- Import `CameraHandler` and `Config`.
- Run a simple loop to render frames with an FPS overlay.
- Ensure the `ESC` key releases the camera and exits cleanly.

## Expected Outcome
Successful verification of the foundation, clearing the path for Phase 2 (Face Registration).

# Step 2: Face Detector Implementation

## Objective
Implement the core logic for detecting human faces within the optimized frame stream.

## Tasks
- Create a `FaceDetector` class in `utils.py`.
- Implement `detect_faces()` method using `face_recognition.face_locations`.
- Ensure the method handles the RGB conversion internally.

## Expected Outcome
A robust detector class that returns a list of detected face coordinates.

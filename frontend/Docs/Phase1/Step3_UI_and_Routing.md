# Step 3: Core UI & Routing Setup

- [x] Build reusable components: `Button.jsx`, `Card.jsx`.
- [x] Build `MainLayout.jsx` with a responsive sidebar.
- [x] Implement routing using `react-router-dom` in `AppRoutes.jsx`.
- [x] Create placeholder pages for Home, Dashboard, Attendance, Register, and Records.
